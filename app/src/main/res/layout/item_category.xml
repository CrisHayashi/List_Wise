<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:layout_marginVertical="6dp">

    <!-- Nome da Categoria -->
    <TextView
        android:id="@+id/txtCategoryName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/categoria"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@color/colorSurface"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btnExpand"
        android:ellipsize="end"
        android:maxLines="1"/>

    <!-- Botão de expandir/retrair -->
    <ImageView
        android:id="@+id/btnExpand"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/expandir_categoria"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ico_expand"
        app:layout_constraintEnd_toStartOf="@+id/btnAddItemToCategory"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@+id/txtCategoryName"
        android:layout_marginEnd="8dp"/>

    <!-- Botão Adicionar Item -->
    <ImageButton
        android:id="@+id/btnAddItemToCategory"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:contentDescription="@string/adicionar_item"
        android:src="@drawable/ico_add_list"
        android:background="?attr/selectableItemBackgroundBorderless"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/txtCategoryName"
        app:layout_constraintBottom_toBottomOf="@+id/txtCategoryName"
        android:layout_marginEnd="8dp"/>

    <!-- Lista de Itens da Categoria -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewItems"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:nestedScrollingEnabled="false"
        android:paddingHorizontal="8dp"
        app:layout_constraintTop_toBottomOf="@+id/txtCategoryName"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Linha separadora -->
    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="#DDDDDD"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        app:layout_constraintTop_toBottomOf="@+id/recyclerViewItems"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>